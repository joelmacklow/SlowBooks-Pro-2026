services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-slowbooks}
      POSTGRES_USER: ${POSTGRES_USER:-slowbooks}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD?set POSTGRES_PASSWORD in your .env or environment}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-slowbooks} -d ${POSTGRES_DB:-slowbooks}"]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    build:
      context: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:-}
      POSTGRES_HOST: ${POSTGRES_HOST:-postgres}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_DB: ${POSTGRES_DB:-slowbooks}
      POSTGRES_USER: ${POSTGRES_USER:-slowbooks}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD?set POSTGRES_PASSWORD in your .env or environment}
      POSTGRES_SSLMODE: ${POSTGRES_SSLMODE:-disable}
      APP_HOST: 0.0.0.0
      APP_PORT: ${APP_PORT:-3001}
      APP_DEBUG: ${APP_DEBUG:-false}
      PYTHONPATH: /app
    ports:
      - "${APP_PORT:-3001}:3001"
    working_dir: /app
    restart: unless-stopped
    command: ["python", "-c", "import sys; sys.path.insert(0, '/app'); from app.container_entrypoint import main; main()"]

  invoice_reminder_scheduler:
    build:
      context: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:-}
      POSTGRES_HOST: ${POSTGRES_HOST:-postgres}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_DB: ${POSTGRES_DB:-slowbooks}
      POSTGRES_USER: ${POSTGRES_USER:-slowbooks}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD?set POSTGRES_PASSWORD in your .env or environment}
      POSTGRES_SSLMODE: ${POSTGRES_SSLMODE:-disable}
      APP_HOST: 0.0.0.0
      APP_PORT: ${APP_PORT:-3001}
      APP_DEBUG: ${APP_DEBUG:-false}
      RUN_INVOICE_REMINDER_SCHEDULER: "true"
      PYTHONPATH: /app
    working_dir: /app
    restart: unless-stopped
    command: ["python", "-c", "import sys; sys.path.insert(0, '/app'); from app.container_entrypoint import main; main()"]

volumes:
  postgres_data:
